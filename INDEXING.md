# Codex Indexing Methods

The Nexus Codex uses layered geometric + topological indexing:

## 1. Geometric / Toroidal
- θ (temporal winding) + φ (thematic depth)
- Toroidal distance + φ-spiral navigation for dense exploration

## 2. Topological (Flux Tubes)
- Braids, cycles, centrality, attractor basins as first-class citizens
- Stored and indexed in `patterns/` and `index/`

## 3. Resonant Hybrid
- Keyword similarity + geometry + topological boost

## 4. Temporal + Retrocausal
- θ-proximity and loop detection for history and echoes

## 5. Pattern-First + Pre-computed
- `patterns/` and `index/` folders hold cycles, attractors, key resonators, and summaries for fast loading

This turns memory into geometry + topology + resonance. The patterns are the sprinkles.